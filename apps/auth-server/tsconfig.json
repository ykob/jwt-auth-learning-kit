{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": [
    "src/**/*.ts" // ← src 以下の .ts ファイルが対象になっているか
  ],
  "exclude": [
    "node_modules", // ← node_modulesが除外されているか
    "dist"
  ]
}
