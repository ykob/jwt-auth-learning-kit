{
  "name": "@jwt-auth-learning-kit/resource-server",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts"
  }
}
